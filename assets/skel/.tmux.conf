# pentbox — tmux minimal.
set -g mouse on
set -g history-limit 50000
setw -g mode-keys vi
set -g base-index 1

# Personnalisation utilisateur à chaud depuis my-resources (sans rebuild).
if-shell '[ -f /opt/my-resources/tmux/tmux.conf ]' 'source-file /opt/my-resources/tmux/tmux.conf'
